"""
Створити програму яка поверне заданий список в зворотному порядку. Реалізувати
рішення використовуючи рекурсію.
"""
lst = [1, 2, 3, 4, 5]


def rev(l):
    if len(l) == 0:
        return []
    return [l[-1]] + rev(l[:-1])


print(rev(lst))
